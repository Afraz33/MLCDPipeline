pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Execute shell command to echo a message
                sh 'echo "Hello, this is a test job from Jenkins!"'
            }
        }
    }
}
